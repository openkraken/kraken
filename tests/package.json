{
  "private": true,
  "scripts": {
    "pretest": "npm install",
    "compile:ts": "cd integration && tsc --outDir ./dist",
    "test": "flutter pub get && flutter clean && npm run integration",
    "integration": "npm run compile:ts && KRAKEN_LIBRARY_PATH=`pwd`/../targets/darwin/debug/lib/ flutter drive --target=integration/app.dart --driver=integration/app_test.dart -d macos"
  },
  "devDependencies": {
    "chalk": "^3.0.0",
    "glob": "^7.1.6"
  },
  "dependencies": {
    "typescript": "^3.8.3"
  }
}
