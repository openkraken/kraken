{
  "private": true,
  "scripts": {
    "install": "flutter pub get",
    "test": "npm run test:flutter",
    "test:flutter": "flutter clean && flutter drive --target=test_driver/app.dart --verbose"
  }
}
