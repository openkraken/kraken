{
  "private": true,
  "scripts": {
    "test": "flutter clean && flutter pub get && npm run unit && npm run integration",
    "unit": "node ./scripts/bootstrap_unit_test",
    "integration": "flutter drive --target=integration/app.dart --driver=integration/app_test.dart -d macos"
  },
  "devDependencies": {
    "chalk": "^3.0.0",
    "glob": "^7.1.6"
  }
}
