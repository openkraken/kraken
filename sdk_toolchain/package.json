{
  "private": true,
  "scripts": {
    "build:android": "cd sdk && cd .android && ./gradlew assembleRelease",
    "upload:android": "cd sdk && cd .android && ./gradlew assembleRelease uploadArchives"
  }
}